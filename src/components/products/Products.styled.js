import styled from "styled-components";

export const Style = {
    Container: styled.div`
        display: flex;
        padding: 20px;
        justify-content: space-between;
        flex-wrap: wrap;
   `,
};
